<div id="navig_fish">

	{% for detailpoisson in detailpoissons %}
		<a href="index.php?route={{detailpoisson.nom}}">
			<img class="{{ namepoisson == detailpoisson.nom ? 'poisson_actif' : '' }}" src="{{detailpoisson.image}}" alt="{{detailpoisson.nom}}"/>
		</a>
	{% endfor %}
</div>
